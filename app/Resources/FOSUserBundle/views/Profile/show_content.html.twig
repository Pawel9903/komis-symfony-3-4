{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">
    <p>Nazwa profilu: {{ user.username }}</p>
    <p>Email: {{ user.email }}</p>
    <a class="btn btn-outline-danger" href="{{ url('fos_user_profile_edit') }}">Edytuj</a>
</div>
